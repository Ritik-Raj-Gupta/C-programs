// declare all the variables first
// scanf syntax--> scanf("only formats",variable names);
// for message use printf before scanf
#include<stdio.h>
int main(){
	int a,b;
	printf("Enter values of a and b:");
	scanf("%d %d",&a,&b);
	printf("sum--> %d ",a+b);
	return 0;
}
