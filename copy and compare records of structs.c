/*
To copy data:
		s1=s where s and s1 are objects of struct
		it copies all the data of s to s1
		> Simple assignment operator on object
*/

/*
To compare data:
			> Use comparison opertors between the attributes being compared "var.attribute > va2.attribute;"
*/

